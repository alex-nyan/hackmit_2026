import { SharedWorkspace } from "@/features/officer-instance/SharedWorkspace";
export const metadata = { title: "Paw Patrol · dispatch" };
export default function WorkspacePage() {
  return <SharedWorkspace role="dispatch" />;
}
